{
  "name": "3d-lpi-player",
  "version": "1.2.0",
  "description": "Lenticular LPI Player for Android",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p www && cp -r index.html manifest.json index.tsx App.tsx types.ts components/ www/ && echo 'Build: Todos os assets copiados para www/'",
    "cap-add-android": "npx cap add android",
    "cap-sync": "npx cap sync android",
    "cap-open-android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}